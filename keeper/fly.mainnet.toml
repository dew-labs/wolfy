app = 'satoru-keeper'
primary_region = 'sin'


[build]
  dockerfile = "./Dockerfile"

[http_service]
  auto_start_machines = true
  min_machines_running = 1
  max_machines_running = 1
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
