{
    "name": "indexer",
    "type": "module",
    "scripts": {
        "start": "bun src/index.ts",
        "dev": "bun --watch src/index.ts",
        "dev-sepolia:start": "mise -P dev-sepolia x -- bun run start",
        "dev-sepolia:dev": "mise -P dev-sepolia x -- bun run dev",
        "stag-sepolia:start": "mise -P stag-sepolia x -- bun run start",
        "stag-sepolia:dev": "mise -P stag-sepolia x -- bun run dev",
        "prod-sepolia:start": "mise -P prod-sepolia x -- bun run start",
        "prod-sepolia:dev": "mise -P prod-sepolia x -- bun run dev"
    },
    "devDependencies": {
        "@types/bun": "^1.2.4"
    },
    "dependencies": {
        "@apollo/server": "^4.11.3",
        "@freyr/shared": "workspace:*",
        "@snapshot-labs/checkpoint": "^0.1.0-beta.44"
    }
}
